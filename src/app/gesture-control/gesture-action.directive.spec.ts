import { GestureActionDirective } from './gesture-action.directive';

describe('GestureActionDirective', () => {
  it('should create an instance', () => {
    const directive = new GestureActionDirective();
    expect(directive).toBeTruthy();
  });
});
